packed struct Sensor {
    id: i32;
    val: i32;
}

fn main() -> i32 {
    volatile let config = 1;
    let base = 1000;
    let reading = base.val;
    
    linear let resource = reading + 1;
    
    if reading {
        let x = resource;
    } else {
        return 1;
    }

    return 0;
}
